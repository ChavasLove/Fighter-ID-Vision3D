# FighterID Vision3D — Python dependencies
# Install with: pip install -r requirements.txt

# Computer vision
opencv-python>=4.8.0
ultralytics>=8.0.0          # YOLOv8 pose detection

# GPU inference — AMD DirectML
onnxruntime-directml>=1.16.0   # ONNX + DirectML para AMD GPU
# Para NVIDIA CUDA: pip install onnxruntime-gpu (reemplaza onnxruntime-directml)

# Supabase
supabase>=2.0.0             # Python client (supabase-py)

# HTTP
requests>=2.31.0

# Utilities
numpy>=1.24.0
scipy>=1.10.0               # Hungarian algorithm (multi-camera matching en main_gui.py)

# Config — carga .env
python-dotenv>=1.0.0

# GUI (requerido para main_gui.py y three_camera_ui.py)
customtkinter>=5.2.0
pillow>=10.0.0

# GPU DirectML — Intel/AMD/NVIDIA en Windows (DirectX 12)
torch-directml>=0.2.0

